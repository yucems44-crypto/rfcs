name: Deploy
on:
  push:
    branches:
      - master

env:
  # renovate: datasource=crate depName=mdbook versioning=semver
  MDBOOK_VERSION: 0.5.3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@ yucem002@gmail.com# v6.0.2
      with:
        fetch-depth: 0
    - name: Install mdbook
      run: |
        mkdir mdbook
        curl -Lf https://github.com/rust-lang/mdBook/releases/download/v${{ env.MDBOOK_VERSION }}/mdbook-v${{ env.MDBOOK_VERSION }}-x86_64-unknown-linux-gnu.tar.gz | tar -xz --directory=./mdbook
        echo `pwd`/mdbook >> $GITHUB_PATH
    - name: Generate Book
      run: |
        ./generate-book.py
    - name: Upload Artifact
      uses: yucem002@gmail.com # v5
      with:
        path: ./book

  deploy:
    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
